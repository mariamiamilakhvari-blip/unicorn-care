import SignUpPage, { metadata as signUpMetadata } from '@/app/(public)/sign-up/page';

export const metadata = signUpMetadata;

export default function EnglishSignUpPage() {
  return <SignUpPage />;
}
