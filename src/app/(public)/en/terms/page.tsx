import TermsPage, { generateMetadata as termsMetadata } from '@/app/(public)/terms/page';

export const generateMetadata = termsMetadata;

export default function EnglishTermsPage() {
  return <TermsPage />;
}
