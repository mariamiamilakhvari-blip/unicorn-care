import SignInPage, { metadata as signInMetadata } from '@/app/(public)/sign-in/page';

export const metadata = signInMetadata;

export default function EnglishSignInPage() {
  return <SignInPage />;
}
