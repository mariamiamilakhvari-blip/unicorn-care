import ClinicSignUpPage, {
  generateMetadata as signUpMetadata,
} from '@/app/(public)/clinic-sign-up/page';

export const generateMetadata = signUpMetadata;

export default function EnglishClinicSignUpPage() {
  return <ClinicSignUpPage />;
}
